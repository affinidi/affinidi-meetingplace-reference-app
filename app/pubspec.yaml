name: mpx_flutter_reference_app
description: "Affinidi Meeting Place - open-source reference implementation of a chat application based on DIDComm v2.1 messaging protocol"
publish_to: "none" # Remove this line if you wish to publish to pub.dev

resolution: workspace

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.5.7+2

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  app_badge_plus: ^1.2.3
  audio_session: ^0.2.4
  audioplayers: ^6.7.1
  bip39_mnemonic: ^4.0.1
  camera: ^0.12.0+1
  circom_witnesscalc: ^0.0.1-beta.1
  clear_all_notifications: ^1.0.0
  clock: any
  collection: ^1.19.1
  confetti: ^0.8.0
  connectivity_plus: ^7.0.0
  copy_with_extension: any
  cross_file: ^0.3.5
  crypto: ^3.0.7
  cryptography: ^2.7.0
  cupertino_icons: ^1.0.8
  didcomm: ^2.3.4
  dio: ^5.10.0
  drift: ^2.28.1
  file_picker: ^12.0.0-beta.5
  firebase_core: ^4.0.0
  firebase_messaging: ^16.0.0
  flutter:
    sdk: flutter
  flutter_appauth: ^12.0.2
  flutter_cache_manager: ^3.4.1
  flutter_card_swiper: ^7.0.2
  flutter_hooks: ^0.21.2
  flutter_hsvcolor_picker: ^1.5.1
  flutter_localizations:
    sdk: flutter
  flutter_rapidsnark: ^0.0.1-beta.7
  flutter_riverpod: ^3.0.3
  flutter_secure_storage: ^10.3.1
  flutter_svg: ^2.0.9
  flutter_vodozemac: ^0.5.0
  form_field_validator: ^1.1.0
  freezed: ^3.1.0
  freezed_annotation: ^3.1.0
  go_router: ^17.3.0
  go_router_builder: ^4.3.0
  hooks_riverpod: ^3.0.3
  image: ^4.5.4
  image_picker: ^1.2.0
  image_picker_android: ^0.8.13+19
  image_picker_platform_interface: ^2.11.0
  intl: any
  intl_phone_number_input: ^0.7.5
  local_auth: ^2.3.0
  meeting_place_chat: ^0.0.1-dev.58
  meeting_place_core: ^0.0.1-dev.48
  meeting_place_credentials: ^0.0.1-dev.13
  meeting_place_drift_repository: ^0.0.1-dev.58
  meeting_place_livekit_flutter: 
    path: ../packages/meeting_place_livekit_flutter
  meeting_place_matrix: ^0.0.1-dev.11
  meeting_place_personal_agent:
    git:  
      url: https://github.com/affinidi/affinidi-meetingplace-sdk-dart.git
      path: packages/meeting_place_personal_agent
      ref: main
  meeting_place_vta_client:
    git:  
      url: https://github.com/affinidi/affinidi-meetingplace-sdk-dart.git
      path: packages/meeting_place_vta_client
      ref: main
  mobile_scanner: ^7.2.0
  mpx_app_core:
    path: ../packages/app-core
  open_filex: ^4.5.0
  package_info_plus: ^10.1.0
  path: ^1.9.1
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  pie_menu: ^3.3.0
  qr_flutter: ^4.1.0
  record: ^6.2.1
  riverpod: ^3.0.3
  riverpod_annotation: ^3.0.3
  share_plus: ^13.1.0
  shared_preferences: ^2.5.5
  skeletonizer: ^2.1.3
  smooth_page_indicator: ^1.2.1
  sqflite_sqlcipher: ^3.4.0
  sqlcipher_flutter_libs: ^0.6.8
  sqlite3: ^2.9.0
  ssi: ^3.9.4
  stack_trace: ^1.12.1
  synchronized: ^3.4.0
  uuid: ^4.5.1
  vc_zkp: ^1.2.5
  video_player: ^2.11.1
  web_socket_channel: ^3.0.3

dependency_overrides:
  didcomm: ^2.3.4

dev_dependencies:
  build_runner: ^2.15.0
  copy_with_extension_gen: any
  dart_flutter_team_lints: ^3.5.2
  drift_dev: ^2.28.0
  fake_async: ^1.3.1
  file: ^7.0.1
  flutter_test:
    sdk: flutter
  riverpod_generator: ^3.0.3

flutter:
  generate: true
  config:
    enable-swift-package-manager: false

  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/videos/
    - assets/zkp/

