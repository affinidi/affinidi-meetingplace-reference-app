name: mpx_app_core
description: "A helper package holding shared classes"
version: 1.0.0
homepage:
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  meeting_place_chat: ^1.0.0
  meeting_place_core: ^1.0.0
  meeting_place_credentials: ^1.0.0

dev_dependencies:
  dart_flutter_team_lints: ^3.3.0
  flutter_test:
    sdk: flutter

flutter:
